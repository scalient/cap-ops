#!/usr/bin/env bash

prefix=<%= prefix.shellescape %>

<% if respond_to?(:rbenv_version) %>
rbenv="rbenv exec"

export -- RBENV_VERSION=<%= rbenv_version.shellescape %>
<% else %>
rbenv=""
<% end %>

exec -- $rbenv chef-client -c "${prefix}/etc/chef-client/client.rb" "$@"
